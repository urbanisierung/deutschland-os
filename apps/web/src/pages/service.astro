---
import { ServiceFinderForm } from "../components/ServiceFinderForm";
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Deutschland OS · Betriebssuche</title>
  </head>
  <body>
    <main>
      <header>
        <h1>Betriebssuche</h1>
        <p>
          Findet für einen konkreten Auftrag (z. B. Wärmepumpe) den passenden lokalen Betrieb —
          gewichtet nach Förderfähigkeit (KfW/BAFA), Entfernung und Zertifizierungen — und entwirft
          die Anfrage an den besten Treffer.
        </p>
      </header>
      <ServiceFinderForm client:load />
    </main>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: light dark;
    --bg: #0f1115;
    --card: #1a1d24;
    --fg: #e7e9ee;
    --muted: #9aa3b2;
    --accent: #ffce00;
    --border: #2a2f3a;
  }
  * {
    box-sizing: border-box;
  }
  body {
    margin: 0;
    font-family: system-ui, sans-serif;
    background: var(--bg);
    color: var(--fg);
    line-height: 1.5;
  }
  main {
    max-width: 720px;
    margin: 0 auto;
    padding: 2rem 1rem 4rem;
  }
  header h1 {
    margin: 0 0 0.25rem;
  }
  header p {
    color: var(--muted);
    margin-top: 0;
  }
  .card {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  fieldset {
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1rem 1.25rem 1.25rem;
    background: var(--card);
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  legend {
    padding: 0 0.5rem;
    font-weight: 600;
  }
  .field {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    font-size: 0.9rem;
  }
  .field span {
    color: var(--muted);
  }
  input,
  select,
  textarea {
    background: #0f1115;
    color: var(--fg);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.5rem 0.6rem;
    font: inherit;
  }
  .checkbox {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  button.primary {
    background: var(--accent);
    color: #111;
    border: none;
    border-radius: 10px;
    padding: 0.75rem;
    font-weight: 700;
    cursor: pointer;
  }
  button.primary:disabled {
    opacity: 0.6;
    cursor: progress;
  }
  .error {
    color: #ff6b6b;
  }
  .result {
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.25rem;
    background: var(--card);
  }
  .ranked {
    border-top: 1px solid var(--border);
    padding-top: 0.75rem;
    margin-top: 0.75rem;
  }
  .meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
  }
  .provider-name {
    font-weight: 600;
    margin-right: auto;
  }
  .badge {
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.15rem 0.6rem;
    font-size: 0.85rem;
  }
  .badge.go {
    border-color: #3ddc84;
    color: #3ddc84;
  }
  .badge.stop {
    border-color: #ff6b6b;
    color: #ff6b6b;
  }
  .flags {
    color: var(--accent);
    margin: 0.5rem 0 0;
    padding-left: 1.1rem;
  }
  pre {
    white-space: pre-wrap;
    word-break: break-word;
    background: #0f1115;
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1rem;
    font: inherit;
  }
</style>
